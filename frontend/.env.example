# URL of the deployed backend API.
# For local development this points to the backend running via Docker or uvicorn.
# On Vercel, add this as an Environment Variable in the project settings.
VITE_API_URL=http://localhost:8000
